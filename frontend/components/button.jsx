import { StyleSheet, Text, View } from "react-native";

const button = () => {
  return (
    <View>
      <Text>button</Text>
    </View>
  );
};

export default button;

const styles = StyleSheet.create({});
